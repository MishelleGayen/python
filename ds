#program to compute area of circle
#imports math library
import math

r=float(input('Input the radius of the circle:'))
#math.pi imports value of pi from math library
a=r*r*(math.pi)
print('The area of the circle with radius',r,'is:',a)

#program to print extention of filename

f = input("Input the Filename: ")
f_extns = f.split(".")
print ("The extension of the file is : " + repr(f_extns[-1]))
